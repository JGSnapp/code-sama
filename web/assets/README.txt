Drop ambient .mp3 files here to populate the in-OS Music Player.

Expected paths (referenced in server/state.py APP_DEFAULTS):
  /assets/music/lofi.mp3
  /assets/music/synth.mp3

If the files are missing the Player still renders — playback just stays
silent.
