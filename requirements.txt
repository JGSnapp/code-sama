fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0
langgraph>=0.2
langchain-openai>=0.2
langchain-core>=0.3
playwright>=1.48
pydantic>=2.7
pyyaml>=6.0
httpx>=0.27
python-multipart>=0.0.9

# OmniVoice TTS (optional — install the local checkout + torch):
#   pip install -e OmniVoice
# soundfile is pulled in by omnivoice; listed here so /tts can encode WAV
# even if you only partially install deps.
soundfile>=0.12
numpy>=1.26
